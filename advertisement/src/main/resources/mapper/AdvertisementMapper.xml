<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.advertisement.mapper.AdvertisementMapper">
    
    <!-- 新增广告 -->
    <insert id="insertAdvertisement" parameterType="com.example.advertisement.domain.entity.Advertisement">
        insert into advertisement (title, content, image_url, target_url, start_time, end_time, status, create_time, update_time)
        values (#{title}, #{content}, #{imageUrl}, #{targetUrl}, #{startTime}, #{endTime}, #{status}, #{createTime}, #{updateTime})
    </insert>
    
    <!-- 根据广告ID查询广告信息 -->
    <select id="selectAdvertisementById" parameterType="Long" resultType="com.example.advertisement.domain.entity.Advertisement">
        select advertisement_id, title, content, image_url, target_url, start_time, end_time, status, create_time, update_time
        from advertisement
        where advertisement_id = #{advertisementId}
    </select>
    
    <!-- 查询广告列表 -->
    <select id="selectAdvertisementList" parameterType="com.example.advertisement.domain.entity.Advertisement" resultType="com.example.advertisement.domain.entity.Advertisement">
        select advertisement_id, title, content, image_url, target_url, start_time, end_time, status, create_time, update_time
        from advertisement
        <where>
            <if test="advertisementId != null">
                and advertisement_id = #{advertisementId}
            </if>
            <if test="title != null and title != ''">
                and title like concat('%', #{title}, '%')
            </if>
             <if test="status != null">
                and status = #{status}
            </if>
            <if test="startTime != null">
                and start_time &gt;= #{startTime}
            </if>
            <if test="endTime != null">
                and end_time &lt;= #{endTime}
            </if>
        </where>
    </select>
    
    <!-- 更新广告信息 -->
    <update id="updateAdvertisement" parameterType="com.example.advertisement.domain.entity.Advertisement">
        update advertisement
        <set>
            <if test="title != null and title != ''">
                title = #{title},
            </if>
            <if test="content != null and content != ''">
                content = #{content},
            </if>
            <if test="imageUrl != null and imageUrl != ''">
                image_url = #{imageUrl},
            </if>
            <if test="targetUrl != null and targetUrl != ''">
                target_url = #{targetUrl},
            </if>
            <if test="startTime != null">
                start_time = #{startTime},
            </if>
            <if test="endTime != null">
                end_time = #{endTime},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            update_time = #{updateTime}
        </set>
        where advertisement_id = #{advertisementId}
    </update>
    
    <!-- 删除广告 -->
    <delete id="deleteAdvertisementById" parameterType="Long">
        delete from advertisement
        where advertisement_id = #{advertisementId}
    </delete>
    
    <!-- 批量删除广告 -->
    <delete id="deleteAdvertisementByIds" parameterType="Long[]">
        delete from advertisement
        where advertisement_id in
        <foreach item="advertisementId" collection="array" open="(" separator="," close=")">
            #{advertisementId}
        </foreach>
    </delete>
    
    <!-- 更新广告点击次数 -->
    <update id="incrementClickCount" parameterType="Long">
        update advertisement
        set click_count = click_count + 1
        where advertisement_id = #{advertisementId}
    </update>
</mapper>