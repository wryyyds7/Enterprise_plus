<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.forum.mapper.ForumFavoriteMapper">
    
    <!-- 新增论坛收藏记录 -->
    <insert id="insertForumFavorite" parameterType="com.example.forum.domain.entity.ForumFavorite">
        insert into forum_favorite (topic_id, user_id, create_time)
        values (#{topicId}, #{userId}, #{createTime})
    </insert>
    
    <!-- 根据收藏ID查询论坛收藏记录信息 -->
    <select id="selectForumFavoriteById" parameterType="Long" resultType="com.example.forum.domain.entity.ForumFavorite">
        select favorite_id, topic_id, user_id, create_time
        from forum_favorite
        where favorite_id = #{favoriteId}
    </select>
    
    <!-- 查询论坛收藏记录列表 -->
    <select id="selectForumFavoriteList" parameterType="com.example.forum.domain.entity.ForumFavorite" resultType="com.example.forum.domain.entity.ForumFavorite">
        select favorite_id, topic_id, user_id, create_time
        from forum_favorite
        <where>
            <if test="favoriteId != null">
                and favorite_id = #{favoriteId}
            </if>
            <if test="topicId != null">
                and topic_id = #{topicId}
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
        </where>
    </select>
    
    <!-- 删除论坛收藏记录 -->
    <delete id="deleteForumFavoriteById" parameterType="Long">
        delete from forum_favorite
        where favorite_id = #{favoriteId}
    </delete>
    
    <!-- 批量删除论坛收藏记录 -->
    <delete id="deleteForumFavoriteByIds" parameterType="Long[]">
        delete from forum_favorite
        where favorite_id in
        <foreach item="favoriteId" collection="array" open="(" separator="," close=")">
            #{favoriteId}
        </foreach>
    </delete>
    
    <!-- 根据主题ID和用户ID查询收藏记录 -->
    <select id="selectByTopicIdAndUserId" parameterType="java.util.Map" resultType="com.example.forum.domain.entity.ForumFavorite">
        select favorite_id, topic_id, user_id, create_time
        from forum_favorite
        where topic_id = #{topicId} and user_id = #{userId}
    </select>
    
    <!-- 根据主题ID统计收藏人数 -->
    <select id="countByTopicId" parameterType="Long" resultType="int">
        select count(*) from forum_favorite
        where topic_id = #{topicId}
    </select>
    
    <!-- 根据用户ID查询收藏主题列表 -->
    <select id="selectFavoriteListByUserId" parameterType="Long" resultType="com.example.forum.domain.entity.ForumFavorite">
        select favorite_id, topic_id, user_id, create_time
        from forum_favorite
        where user_id = #{userId}
    </select>
</mapper>