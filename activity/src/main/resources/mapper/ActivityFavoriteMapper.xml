<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.activity.mapper.ActivityFavoriteMapper">
    
    <!-- 新增活动收藏 -->
    <insert id="insertActivityFavorite" parameterType="com.example.activity.domain.entity.ActivityFavorite">
        insert into activity_favorite (activity_id, user_id, create_time)
        values (#{activityId}, #{userId}, #{createTime})
    </insert>
    
    <!-- 根据收藏ID查询活动收藏信息 -->
    <select id="selectActivityFavoriteById" parameterType="Long" resultType="com.example.activity.domain.entity.ActivityFavorite">
        select favorite_id, activity_id, user_id, create_time
        from activity_favorite
        where favorite_id = #{favoriteId}
    </select>
    
    <!-- 查询活动收藏列表 -->
    <select id="selectActivityFavoriteList" parameterType="com.example.activity.domain.entity.ActivityFavorite" resultType="com.example.activity.domain.entity.ActivityFavorite">
        select favorite_id, activity_id, user_id, create_time
        from activity_favorite
        <where>
            <if test="favoriteId != null">
                and favorite_id = #{favoriteId}
            </if>
            <if test="activityId != null">
                and activity_id = #{activityId}
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
        </where>
    </select>
    
    <!-- 删除活动收藏 -->
    <delete id="deleteActivityFavoriteById" parameterType="Long">
        delete from activity_favorite
        where favorite_id = #{favoriteId}
    </delete>
    
    <!-- 批量删除活动收藏 -->
    <delete id="deleteActivityFavoriteByIds" parameterType="Long[]">
        delete from activity_favorite
        where favorite_id in
        <foreach item="favoriteId" collection="array" open="(" separator="," close=")">
            #{favoriteId}
        </foreach>
    </delete>
    
    <!-- 根据活动ID和用户ID查询收藏记录 -->
    <select id="selectByActivityIdAndUserId" parameterType="java.util.Map" resultType="com.example.activity.domain.entity.ActivityFavorite">
        select favorite_id, activity_id, user_id, create_time
        from activity_favorite
        where activity_id = #{activityId} and user_id = #{userId}
    </select>
    
    <!-- 根据活动ID统计收藏人数 -->
    <select id="countByActivityId" parameterType="Long" resultType="int">
        select count(*) from activity_favorite
        where activity_id = #{activityId}
    </select>
    
    <!-- 根据用户ID查询收藏活动列表 -->
    <select id="selectFavoriteListByUserId" parameterType="Long" resultType="com.example.activity.domain.entity.ActivityFavorite">
        select favorite_id, activity_id, user_id, create_time
        from activity_favorite
        where user_id = #{userId}
    </select>
</mapper>